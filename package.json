{
  "name": "oilspill-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first maritime oil spill investigation prototype for SIH",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test tests/*.test.js",
    "reset-demo": "node scripts/reset-demo.js"
  },
  "engines": { "node": ">=20" }
}
